## 1. Index section headers

- [x] 1.1 Change "Activos" → "Active Changes" in `renderIndexContent()`
- [x] 1.2 Change "Archivados" → "Archived Changes" in `renderIndexContent()`
- [x] 1.3 Change "Specs" → "Specifications" in `renderIndexContent()`

## 2. Index empty-state messages

- [x] 2.1 Change "No hay changes activos" → "No active changes" in `renderIndexContent()`
- [x] 2.2 Change "No hay changes archivados" → "No archived changes" in `renderIndexContent()`
- [x] 2.3 Change "No hay specs disponibles" → "No specifications available" in `renderIndexContent()`

## 3. Helpbars

- [x] 3.1 Change `ModeIndex` helpbar: `j/k: navegar  Enter: abrir  Esc: salir` → `j/k: navigate  Enter: open  Esc: quit` in `renderHelpBar()`
- [x] 3.2 Change `ModeViewingArchive` helpbar: `1-4: artifact  j/k: scroll  a/Esc: índice  q: salir` → `1-4: artifact  j/k: scroll  a/Esc: index  q: quit` in `renderHelpBar()`
- [x] 3.3 Change `ModeViewingSpec` helpbar: `j/k: scroll  Esc: índice  q: salir` → `j/k: scroll  Esc: index  q: quit` in `renderHelpBar()`
- [x] 3.4 Change tasks helpbar (ModeNormal): `j/k: navegar  Space: toggle  e: editar  Esc: índice` → `j/k: navigate  Space: toggle  e: edit  Esc: index` in `renderHelpBar()`
- [x] 3.5 Change proposal/design/specs helpbar (ModeNormal): `j/k: scroll  e: editar  Esc: índice` → `j/k: scroll  e: edit  Esc: index` in `renderHelpBar()`

## 4. Header and welcome screen

- [x] 4.1 Change "índice" → "index" in the `ModeIndex` header in `renderHeader()`
- [x] 4.2 Change `[archivo]` → `[archive]` in the `ModeViewingArchive` header in `renderHeader()`
- [x] 4.3 Change welcome screen message: "No hay changes activos. Crea uno con /opsx:propose" → "No active changes. Create one with /opsx:propose" in `emptyView()`
