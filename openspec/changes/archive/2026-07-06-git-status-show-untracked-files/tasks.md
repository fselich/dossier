## 1. Core Implementation

- [x] 1.1 Add `-u` flag to `git status --porcelain` in `internal/git/git.go`

## 2. Tests

- [x] 2.1 No existing tests for the git package — skipped

## 3. Verification

- [x] 3.1 Run `make test` to confirm all tests pass
- [x] 3.2 Run `make lint` to confirm no linting issues
