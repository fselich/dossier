## 1. Implementation

- [x] 1.1 Add header click handling in `handleMouseClick`: check `msg.Y == 1` for `ModeNormal` and `ModeViewingArchive`, call `m.enterIndex()`

## 2. Tests

- [x] 2.1 Add test cases: click header in normal mode enters index, click header in archive view enters index, click header in index mode does nothing, click header in spec viewer does nothing
